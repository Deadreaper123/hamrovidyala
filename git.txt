Always run this code first

	1) git pull

Then

	2) git add . (add all changes)
	3) git commit -m "enter a message" (commits the change to local git )
	3) git push -u origin main (commits it to the global git )
     